public class ReverseLinkedListTemplate {
    static class ListNode {
        int val;
        ListNode next;

        ListNode(int val) {
            this.val = val;
        }
    }

    /*
     * Template idea:
     * - use previous, current, next
     * - reverse pointer direction one node at a time
     * - return new head at the end
     */
}
