public class Problem01_BinarySearch {
    /*
     * Hint:
     * Maintain left and right bounds on the sorted array.
     * Repeatedly check the middle element.
     * Pseudocode:
     * left = 0
     * right = nums.length - 1
     * while left <= right:
     *     mid = left + (right - left) / 2
     *     if nums[mid] == target:
     *         return mid
     *     else if nums[mid] < target:
     *         search right half
     *     else:
     *         search left half
     * return -1
     */
    public static void main(String[] args) {
        
    }
    
}
